function solution(n) {
    var answer = [];
    var array = n.toString().split('').map(Number);
    for(i=n.toString().length-1;i>=0;i--)
        {
            answer.push(array[i]);
        }
    return answer;
}
